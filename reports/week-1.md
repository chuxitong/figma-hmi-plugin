# Неделя 1. Исследование Figma и создание репозитория проекта

Студент: Чжан Сычэн. Неделя по плану задач: репозиторий, заметка о том, какие данные Figma нужны конвейеру, и простейшая проверка сообщений между UI и песочницей плагина.

## Что сделано

- Поднят Git-репозиторий `figma-hmi-plugin`; внутри есть каталоги `figma-plugin` (manifest, `dist/code.js`, UI), заготовки `mockups/` и позже добавленный `local-service/` для инференса.
- В `help/` оформлено описание входов API (PNG, текст задания, опционально `variables`, `css_hints`, `current_code`) и того, какие галки UI их включают; это соответствует требованию недели сообщить не только «мы умеем export», но и **какие поля дальше нужны модели**.
- Проверен минимальный круг сообщений sandbox ↔ UI через `Plugins → Development`; при открытии плашку с build-stamp можно сверять с файлом панели после синхронизации с сервера.

## Где смотреть

| Путь | Содержание |
|------|----------------|
| Корень README / `help/` | Ориентация в структуре |
| `figma-plugin/manifest.json`, `dist/code.js` | Рабочий снимок главного потока плагина |
| `help/docs/figma-data-extraction.zh.md` | То же самое техническими списками (кит.; для меня оператора) |

## Ограничения недели

Полная склейка с UI2Code^N ещё впереди (неделя 3–4); на неделе 1 фокус именно на **протоколе данных Figma**.
