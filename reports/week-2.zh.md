# 第 2 周——工业界面模型集（可复现 PNG）

可复现实验底座在 `mockups/layered-svg/screens/*.svg`，经 `mockups/build_mockups.py` 渲到 `mockups/png/*.png`（1440×900）。与老师沟通时：**Week 8 批量仍用这一套 mockup**；「Figma 真 variables」的结论只来自 Week 7 的 **`figma_native` + `--context-mode from-json`**，两条证据链拆开写。
