# Week 5 — plug-in demonstration recording

## Submitted artefact

The course requirement was a short recording of the plug-in shell talking to `local-service`. The file **`week-5-plugin-shell-demo.mp4`** sits in **`reports/deliverables_week5/`** beside this readme (samebasename with `.webm` or `.gif` is fine if your toolchain exported that instead).

Recording shows Figma Development mode with this repo’s plugin, plus the backend already listening on `:8000`, then **Generate Code** run with logs, code buffer, and preview updating. Refine / Edit appear briefly in the latter half.

## Context

Screens come from the week‑2 Industrial HMI set; full layout sources live on Figma (`reports/week-2.md` cites the canonical design link).
